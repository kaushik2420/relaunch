/**
 * DEPRECATED — no longer used.
 *
 * The resume is now rendered as a real PDF (render-pdf.tsx) and a real
 * .docx (render-docx.ts). The old HTML → Google Doc import approach was
 * dropped because Drive's HTML importer mangles multi-column layouts.
 *
 * This file is intentionally empty and safe to delete.
 */
export {};
